# Styles Exports

This directory stores Site Editor style exports generated by the `bm sync-site-editor` WP-CLI command.

- `site-editor-export.json`: raw global styles JSON exported from the database.
- `site-editor-custom.css`: exported custom CSS, if any.

Treat these files as review artifacts. Promote durable style decisions into `theme.json` or permanent CSS before commit.

